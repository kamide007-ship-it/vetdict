# 疾患データベース拡張 設計提案（実装前）

## 1. 要件整理

### 実装対象（機能リスト）
- 疾患データベース内の**全疾患エントリ**に、以下5項目を必須化する。  
  - 病態生理
  - 予防
  - 治療
  - 原因
  - 予後
- 各項目に対して、提示済みの参考文献リスト（[1]〜[42]）から**出典番号付き引用**を付与する。
- UI上で疾患詳細を表示する際、本文と出典（番号＋書誌）を一貫したフォーマットで提示する。
- API応答でも、本文と出典情報を構造化して返す（監査・再利用可能にする）。
- 「引用不足」「5項目欠落」「出典番号不整合」を検出するバリデーションを追加する。
- 既存ルート/URL/認証/フォーム識別子は変更しない（差分実装）。

### 受け入れ条件（Definition of Done）
- 全疾患で5項目が埋まっている。
- 各項目に1件以上の出典が紐づく。
- 出典番号が固定マスターと一致し、リンク切れや番号ズレがない。
- 既存画面・既存APIが破壊されていない（後方互換）。

### 不足情報（実装前に確認したい点）
1. 「引用」は**逐語引用**（原文短文）まで必要か、または**要約＋出典番号**でよいか。
2. 日本語表記は医学用語の統一辞書（例: 予後=短期/長期）を適用するか。
3. 対象の「全疾患」は現在リポジトリ内の全speciesファイル収載分でよいか。
4. 文献[7][14][15]などWeb系資料は、将来更新時に版管理（取得日）を固定するか。

## 2. コンポーネント設計

### UI分割（最小差分）
- `DiseaseDetailPage`（既存詳細表示の親）
  - 役割: 疾患データ取得、エラー/ロード管理、子へデータ受け渡し
  - Props: `diseaseId`（または slug）
  - State: `loading`, `error`, `diseaseRecord`
- `DiseaseCoreSections`
  - 役割: 5項目（病態生理/予防/治療/原因/予後）をセクション表示
  - Props: `coreSections`（5項目の本文＋引用配列）
  - State: なし（純表示）
- `CitationList`
  - 役割: セクション単位の引用番号リスト表示、重複除去、ホバー/クリックで書誌表示
  - Props: `citations[]`, `citationMaster`
  - State: `expanded`（必要なら）
- `EvidenceQualityBadge`（任意）
  - 役割: 引用数・資料種別（教科書/論文/ガイド）に応じた品質表示
  - Props: `evidenceMeta`
  - State: なし

## 3. データ設計・状態管理

### 推奨データモデル
```json
{
  "id": "canine_ckd",
  "species": "dog",
  "name_ja": "慢性腎臓病",
  "sections": {
    "pathophysiology": {
      "text": "...",
      "citations": [1, 2, 27]
    },
    "prevention": {
      "text": "...",
      "citations": [7, 13]
    },
    "treatment": {
      "text": "...",
      "citations": [2, 12, 27]
    },
    "etiology": {
      "text": "...",
      "citations": [1, 2]
    },
    "prognosis": {
      "text": "...",
      "citations": [2, 27]
    }
  }
}
```

### 出典マスター（固定）
- `reference_catalog` を単一ソースで保持し、番号→書誌情報を解決。
- 疾患データは番号参照のみを持ち、書誌本文を重複保持しない。

### 状態管理方針
- 既存構成を維持しつつ、データ取得後に`schema validation`を実施。
- バリデーション失敗時は
  - API: 監査用エラー詳細を返す（本番UIには汎用メッセージ）
  - UI: 「情報整備中」表示＋ログ送信

## 4. 懸念点とエッジケース

### 技術的懸念
- 既存疾患データが自由形式の場合、5項目への正規化移行で欠損が大量発生する。
- 種別ファイル（犬猫/馬/エキゾ）ごとに記述粒度が不均一で、引用の粒度統一が難しい。
- 参照番号の手入力ミス（重複・存在しない番号）を自動検知しないと品質が崩れる。

### パフォーマンス
- 全疾患読み込み時に引用マージ処理が重い場合、一覧表示で遅延する可能性。
  - 対策: 詳細画面で遅延解決、またはビルド時プリコンパイル。

### エラーハンドリング
- 引用番号が`reference_catalog`に存在しない: フェイルクローズ（表示抑制＋監査ログ）。
- 5項目のいずれか欠落: UIで欠落表示、APIは警告フラグを返却。
- 外部公開URL（文献サイト）不達: ローカル保持の書誌表示を優先。

### 監査観点（本番前）
- 認証・認可を触らないこと（既存ロジック維持）。
- XSS対策として疾患本文はエスケープ表示を維持。
- キャッシュ更新時に古い疾患定義が残留しないよう、データバージョンキーを導入。

---

## 承認依頼
上記設計で問題なければ、次に**差分実装（コード変更）**へ進みます。  
「設計に合意します。実装を開始してください」とご指示ください。
