# 🧪 ShowDog UI Components テストガイド

## 📋 テスト概要

このドキュメントは、新しく実装された症状チェックUI コンポーネントをテストするためのガイドです。

### 🎯 テスト対象

1. ✅ **解析進捗アニメーション** (`analysis-progress.js`)
   - マルチステップ進捗表示
   - リアルタイム進捗更新
   - ステップ状態管理

2. ✅ **疾患カードコンポーネント** (`diagnostic-cards.js`)
   - 展開/折り畳み機能
   - タブインターフェース
   - インタラクティブなアクションボタン

3. ✅ **ケアガイド表示** (`treatment-plan.js`)
   - 包括的なケアガイド情報表示
   - 参考検査と優先度
   - サプリメント推奨
   - スケジュール表示

---

## 🚀 ローカルテスト手順

### ステップ 1: サーバーの起動

```bash
# プロジェクトディレクトリに移動
cd /home/user/showdog-app

# サーバーを起動
python app.py
# または
python3 api/showdog_api.py
```

✅ サーバーが `http://localhost:5000` で起動していることを確認

### ステップ 2: デモページにアクセス

```
URL: http://localhost:5000/components-demo.html
```

画面が読み込まれることを確認します。

### ステップ 3: コンポーネントのテスト

#### 🔍 テスト 1: 解析進捗アニメーション

1. **デモページ** で "デモを実行" ボタン（Progress Animation セクション）をクリック
2. ✅ 期待される動作：
   - 3つのステップが表示される
   - ステップが順番に完了していく
   - プログレスバーがアニメーション表示される
   - 3秒後にアラートが表示される

**チェックリスト:**
- [ ] ステップが正しく表示される
- [ ] プログレスバーがスムーズに進行する
- [ ] アイコンが適切に変更される（⏳ → ⚙️ → ✅）
- [ ] パーセンテージが更新される
- [ ] 完了後にアラートが表示される

#### 🎨 テスト 2: 疾患カード

1. **デモページ** で "デモを実行" ボタン（Disease Cards セクション）をクリック
2. ✅ 期待される動作：
   - 2つの疾患カードが表示される
   - 各カードが "短頭種気道症候群" と "心不全" の情報を表示

**チェックリスト:**
- [ ] カードが表示される
- [ ] 重症度バッジが表示される（色分けされている）
- [ ] 信頼度スコアが表示される
- [ ] カードをクリックすると展開する
- [ ] タブを切り替えられる（Overview → Reasoning → Treatment）
- [ ] 各タブの情報が正確に表示される
- [ ] アクションボタンがクリック可能

**詳細チェック:**

**Overview タブ:**
- [ ] 疾患説明が表示される
- [ ] 症状マッチング情報が表示される
- [ ] 推奨検査が表示される

**Reasoning タブ:**
- [ ] 判定根拠が日本語で表示される
- [ ] 信頼度ファクターが表示される（グラフ付き）
- [ ] 各ファクターのパーセンテージが正確

**Treatment タブ:**
- [ ] ケアガイドが表示される
- [ ] サプリメント情報が表示される
- [ ] 用量、頻度、理由が正確

#### 💊 テスト 3: ケアガイド表示

1. **デモページ** で "デモを実行" ボタン（Treatment Plan セクション）をクリック
2. ✅ 期待される動作：
   - ケアガイドが表示される
   - 参考検査が表示される
   - サプリメント推奨が表示される
   - タイムラインが表示される
   - 経過観察チェックリストが表示される

**チェックリスト:**
- [ ] ヘッダーが表示される
- [ ] 直ちに行うべきことセクションが表示される
- [ ] 参考検査が優先度付きで表示される
- [ ] サプリメント推奨が表示される
- [ ] ケアスケジュール（タイムライン）が表示される
- [ ] 経過観察項目チェックボックスが機能する
- [ ] 医師相談用メモセクションが表示される
- [ ] プリントボタンが機能する（Ctrl+P で印刷プレビュー）

---

## 🔗 統合テスト

### テスト 4: 症状チェック チャット統合

1. ブラウザで `http://localhost:5000/diagnostic-chat.html` にアクセス

2. チャットボックスに症状を入力
   ```
   例: "咳が続いている、呼吸が苦しい、いびきをかいている"
   ```

3. ✅ 期待される動作：
   - AIが症状を認識する
   - サイドパネルに症状が表示される
   - 関連疾患（参考）が候補リストに表示される
   - 関連疾患（参考）をクリックすると展開カードが表示される
   - ケアガイドセクションが自動表示される

**チェックリスト:**
- [ ] チャットメッセージが送信される
- [ ] AIの応答が表示される
- [ ] 抽出症状がサイドパネルに表示される
- [ ] 関連疾患（参考）がリスト表示される
- [ ] 候補をクリック可能
- [ ] 展開カード（diseaseCardsContainer）が表示される
- [ ] ケアガイド（treatmentContainer）が自動表示される
- [ ] スクロールでコンテンツが見える

### テスト 5: API 応答確認

1. ブラウザの開発者ツール（F12）を開く
2. **Network** タブを選択
3. 症状チェック チャットで症状を入力して送信
4. `/api/diagnostic-chat/chat` リクエストを確認

**チェックリスト:**
- [ ] リクエストが送信される
- [ ] ステータスコード 200 が返される
- [ ] レスポンスに以下のフィールドが含まれている：
  - [ ] `disease_candidates`
  - [ ] `reasoning` フィールド
  - [ ] `treatment_recommendations`
  - [ ] `analysis_steps`
  - [ ] `confidence_level`

**レスポンス例:**
```json
{
  "disease_candidates": [
    {
      "disease_id": "brachycephalic_airway_syndrome",
      "name_ja": "短頭種気道症候群",
      "confidence_level": "85%",
      "reasoning": {
        "why_this_condition_ja": "...",
        "confidence_factors": [...]
      },
      "treatment_recommendations": {
        "supplements": [...],
        "diagnostic_tests": [...]
      }
    }
  ]
}
```

---

## 🐛 トラブルシューティング

### ❌ コンポーネントが表示されない

**症状:** ページが空白、またはエラーが表示される

**チェック項目:**
1. ブラウザのコンソールでエラーを確認
   ```
   F12 → Console タブ
   ```
2. スクリプトが正しくロードされているか確認
   ```
   F12 → Network タブ
   ```
3. 以下のファイルが 200 ステータスで読み込まれているか確認：
   - `/js/analysis-progress.js`
   - `/js/diagnostic-cards.js`
   - `/js/treatment-plan.js`
   - `/design-system.css`

**解決方法:**
```bash
# ハードリフレッシュ
Ctrl + Shift + R (Windows/Linux)
Cmd + Shift + R (Mac)
```

### ❌ スタイルが適用されない

**症状:** レイアウトが崩れている、配色がおかしい

**原因:** デザインシステムの CSS が読み込まれていない

**解決方法:**
```bash
# キャッシュをクリア
1. DevTools を開く (F12)
2. ⚙️ Settings → Network → "Disable cache" をチェック
3. ページをリロード
```

### ❌ API エラー

**症状:** "エラーが発生しました" のようなメッセージが表示される

**チェック項目:**
1. サーバーが起動しているか確認
2. ネットワークタブで API レスポンスを確認
3. コンソールでエラーメッセージを確認

**解決方法:**
```bash
# サーバーログを確認
tail -f /path/to/server.log
```

### ❌ カードが展開しない

**症状:** カードをクリックしても何も起こらない

**チェック項目:**
1. JavaScript コンソールでエラーを確認
2. `diagnostic-cards.js` が読み込まれているか確認

**解決方法:**
```javascript
// コンソールで手動テスト
const renderer = new DiseaseCardRenderer('test-container');
console.log(renderer); // オブジェクトが表示されるか確認
```

---

## ✅ テスト完了チェックリスト

すべてのテストが完了したら、以下を確認してください：

### 機能テスト
- [ ] 解析進捗アニメーション - すべてのステップが表示される
- [ ] 疾患カード - すべてのタブが機能する
- [ ] ケアガイド - すべての情報が表示される
- [ ] API 統合 - レスポンスに新しいフィールドが含まれている

### UI/UX テスト
- [ ] アニメーションがスムーズ
- [ ] レイアウトが正しく表示される
- [ ] テキストが読みやすい
- [ ] ボタンがクリック可能

### レスポンシブテスト
- [ ] デスクトップ (1920px) - 3列レイアウト
- [ ] タブレット (768px) - 2列レイアウト
- [ ] モバイル (375px) - 1列レイアウト

### ブラウザテスト
- [ ] Chrome/Chromium
- [ ] Firefox
- [ ] Safari

---

## 📊 テスト結果レポート

テスト実施後、以下のテンプレートで結果を記録してください：

```
## テスト実施日時
[日時を記入]

## テスト環境
- ブラウザ: [Chrome/Firefox/Safari など]
- OS: [Windows/Mac/Linux など]
- サーバー: [URL]

## テスト結果
### ✅ 合格した項目
- [項目1]
- [項目2]

### ❌ 失敗した項目
- [項目1]: [詳細]
- [項目2]: [詳細]

### 🔧 修正が必要な項目
- [項目1]: [修正内容]
- [項目2]: [修正内容]

## 総合評価
[合格/要修正/不合格]

## コメント
[その他のコメント]
```

---

## 📞 問題が発生した場合

1. **コンソールエラーをコピー**
   ```
   F12 → Console → エラーを選択して Ctrl+C
   ```

2. **ネットワークログを確認**
   ```
   F12 → Network → 失敗したリクエストを確認
   ```

3. **サーバーログを確認**
   ```bash
   # ターミナルでサーバーログを確認
   tail -100 server.log
   ```

4. **GitHub Issue を作成**
   - タイトル: 簡潔に説明
   - 説明: 上記の情報を含める
   - ラベル: `bug` または `test-failure`

---

## 📚 関連ドキュメント

- [API ドキュメント](./API.md)
- [コンポーネント使用ガイド](./COMPONENTS.md)
- [実装詳細](./IMPLEMENTATION.md)

---

**テストの実施をお疲れ様でした！** 🎉
